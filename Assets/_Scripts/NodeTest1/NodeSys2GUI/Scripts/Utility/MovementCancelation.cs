using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementCancelation : MonoBehaviour
{
    //will move one object locked to the mouse in one direction and all other objects in the oposite direction
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
